services:
  frontend:
    build: .
    restart: unless-stopped
    ports:
      - "${PORT:-80}:80"
    depends_on:
      - backend

  backend:
    build: ./server
    restart: unless-stopped
    env_file:
      - path: .env
        required: false
    expose:
      - "3002"
